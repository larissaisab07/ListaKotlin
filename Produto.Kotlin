class Produto(var nome: String, var preco: Double, var quantidade: Int) {
    fun calcularTotal(): Double {
        return preco * quantidade
    }

    fun Quantidade(novaQuantidade: Int) {
        quantidade = novaQuantidade
    }
}
